import styled from 'styled-components'
import FormContainer from "../views/Form/form";

const Form = () => {
    return(
        <Container>
            <FormContainer/>
        </Container>
    )
};

const Container = styled.div`

`;

export default Form;
