import styled from 'styled-components'
import LiveContainer from "../views/live/containers/LiveContainer";

const Live = () => {
    return(
        <Container>
            <LiveContainer/>
        </Container>
    )
};

const Container = styled.div`

`;

export default Live;
